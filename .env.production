# Production environment.
# Loaded automatically by vite build (NODE_ENV=production).
# Real values are injected via GitHub Actions secrets — never commit actual values.

VITE_API_BASE_URL=https://dummyjson.com
VITE_SENTRY_DSN=
