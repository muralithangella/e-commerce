# Staging environment.
# Loaded when VITE_MODE=staging (vite build --mode staging).
# Real values injected by CI/CD — never commit actual secrets.

VITE_API_BASE_URL=https://dummyjson.com
VITE_SENTRY_DSN=
