# Base environment — committed to git.
# Contains non-secret defaults shared across all environments.
# Override per-environment in .env.local, .env.staging, .env.production

VITE_APP_NAME=ShopDash
VITE_API_TIMEOUT=10000
